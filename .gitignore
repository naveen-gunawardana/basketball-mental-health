# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Secrets — never commit. outreach/.env holds Gmail app passwords and the
# Resend key; this repo is public.
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Supabase CLI scratch state (version cache, linked project ref)
supabase/.temp/

# Misc
*.log

# Windows reserved device name (created accidentally)
nul
